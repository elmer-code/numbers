<%= link_to "Edit Collection",
            edit_collection_path(@collection) %>

<%= link_to "Delete Collection",
            collection_path(@collection),
            method: :delete,
            confirm: "Delete this Collection?" %>

<%= nested_collections @collection.subtree.arrange(order: :created_at) %>
